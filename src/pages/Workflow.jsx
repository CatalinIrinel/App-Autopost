import { Flex, Heading } from '@chakra-ui/react';
import React from 'react';

const Workflow = () => {
  return (
    <Flex w={'full'} h={'100vh'}>
      <Heading as={'h1'}> Workflow area </Heading>
    </Flex>
  );
};

export default Workflow;
