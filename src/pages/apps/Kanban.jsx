import { Flex, Heading } from '@chakra-ui/react';
import axios from 'axios';
import React from 'react';

const Kanban = () => {
  return (
    <Flex w={'full'} h={'100vh'}>
      <Heading as={'h1'}> Kanban area </Heading>
    </Flex>
  );
};

export default Kanban;
