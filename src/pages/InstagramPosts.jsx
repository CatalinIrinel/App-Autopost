import { Flex, Heading } from '@chakra-ui/react';
import React from 'react';

const InstagramPosts = () => {
  return (
    <Flex w={'full'} h={'100vh'}>
      <Heading as={'h1'}> InstagramPosts area </Heading>
    </Flex>
  );
};

export default InstagramPosts;
