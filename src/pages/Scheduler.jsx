import { Flex, Heading } from '@chakra-ui/react';
import React from 'react';

const Scheduler = () => {
  return (
    <Flex w={'full'} h={'100vh'}>
      <Heading as={'h1'}> Scheduler area </Heading>
    </Flex>
  );
};

export default Scheduler;
